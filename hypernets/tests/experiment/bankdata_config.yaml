version: 2.5
workingDir: "Z:/experiment/2012-17"  # to store experiment files

dataset:
    target: y
    trainFile: C:/Users/wuhf/Documents/datasets/bankdata_1k.csv
    evalFile: C:/Users/wuhf/Documents/datasets/bankdata_1k.csv
    testFile: C:/Users/wuhf/Documents/datasets/bankdata_1k.csv

jobs:
  - name: train-no-seleciton  # should be unique in workingDir or current file
    engine: hypergbm  # job engine, skip the job if engine is not exits
    experiment:
      strategy: evolution
      searcherOptions:
          populationSize: 30
          sampleSize: 30
      earlyStopping:
          rounds: 10
          timeLimit: 36000
          reward: 0.7
      rewardMetric: f1
      posLabel: "yes"
      optimizeDirection: max
      clearCache: false
      logLevel: 20
      evaluation:
        metrics: auto
        persistPrediction: true
        persistPredictionDir:   # str, optional for None , store to {workingDir}/{jobs[i].name}/prediction
      report:
        enable: true
        render: excel
        renderOptions:
            filePath:  # str, optional, for None store to {workingDir}/{jobs[i].name}/report.xlsx
            theme: dark
      featureSelection:
        enable: false
      featureGeneration:
        enable: false
    runOptions:
        max_trials: 2
    webui:
        port: 8888
